# Example environment variables (do NOT commit real secrets)
# Copy this file to .env and fill in values for your environment.

# PayMango (server-side) settings
PAYMANGO_SECRET=
PAYMANGO_WEBHOOK_SECRET=
# Optional: override PayMango API base (use only for testing/mocks)
PAYMANGO_API_BASE=https://api.paymango.com

# Other app settings (example)
app.baseURL=http://localhost:8080
